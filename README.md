# Proofpoint Technical Challenge

This project processes a corrupted episode catalog in CSV format.

## Features
- Parses and normalizes raw input
- Fixes missing and invalid fields
- Detects and removes duplicate episodes
- Generates a cleaned catalog and a quality report

## How to run

```bash
python src/main.py